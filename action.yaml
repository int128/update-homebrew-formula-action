name: hello-world
description: say Hello World

inputs:
  path:
    description: glob path
    required: true
  token:
    description: GitHub token
    required: true

runs:
  using: 'node12'
  main: 'dist/index.js'
