name: hello-world
description: say Hello World

inputs:
  path:
    description: glob path
    required: true
  token:
    description: GitHub token
    required: true

runs:
  using: 'node16'
  main: 'dist/index.js'
